<form method="post" action="{{ path('app_utiliser_delete', {'id': utiliser.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ utiliser.id) }}">
    <button class="btn btn-outline-danger btn-fw">Delete</button>
</form>
