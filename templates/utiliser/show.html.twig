{% extends 'base.html.twig' %}

{% block title %}Utiliser{% endblock %}

{% block body %}
    <h1>Utiliser</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ utiliser.id }}</td>
            </tr>
            <tr>
                <th>Date_utiliser</th>
                <td>{{ utiliser.dateUtiliser ? utiliser.dateUtiliser|date('Y-m-d') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_utiliser_index') }}">back to list</a>

    <a href="{{ path('app_utiliser_edit', {'id': utiliser.id}) }}">edit</a>

    {{ include('utiliser/_delete_form.html.twig') }}
{% endblock %}
